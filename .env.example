# ── Obligatorio ──────────────────────────────────────────────────────────────
# La URL ACP del agente. Es su campo `agentUrl` tal cual (wss://…/acp). Sin esto la app no
# intenta conectar: te lo dice en pantalla en vez de dejarte mirando un spinner.
ACP_WS_URL=wss://acp-<agentId>.sandboxes.easybits.cloud/acp

# El token del agente. Sale de crear el agente: es su `embedToken`, salvo que le hayas puesto
# un `ACP_AGENT_TOKEN` propio en el `env`, y entonces es ése.
#
# ⚠️ NO es el GOOSE_SERVER__SECRET_KEY de la caja, como decía este archivo hasta hoy: ése es un
# secreto interno que se regenera en cada arranque y nunca sale de la microVM.
# Se manda como `?token=` en la URL y como `Authorization: Bearer`. Déjalo vacío si tu agente
# ACP no pide credencial.
ACP_TOKEN=

# ── Opcional ─────────────────────────────────────────────────────────────────
# Dónde abre sus sesiones el agente. En una caja ghosty-lite, /data/work es lo que existe y lo
# único que sobrevive a que se duerma; si pones una ruta que no exista dentro, el agente la
# degrada a /data/work sin avisar.
ACP_CWD=/data/work

# Cuánto esperar (ms) a que el agente abra o reabra una sesión. Incluye arrancar las
# extensiones MCP activas: una que no responda ya no deja la conversación colgada.
ACP_SESSION_TIMEOUT_MS=60000

# Ciclo de vida de la caja (SÓLO EasyBits): despertarla al hablarle y suspenderla al ocio.
# Sin estas dos, esto es un cliente ACP normal contra cualquier proveedor: se conecta, pero no
# despierta nada — el agente tiene que estar ya arriba.
AGENT_BOX_ID=
EASYBITS_API_KEY=

# Para recrear la caja si el host la perdió. Ojo: la caja nueva tiene OTRA URL, así que habrá
# que actualizar ACP_WS_URL a mano.
AGENT_SNAPSHOT_ID=

# Modelos que el gateway sirve pero el agente no lista en su selector.
# goose arma el select con los modelos que conoce de su proveedor y esa lista se queda corta:
# el gateway de EasyBits sirve `deepseek-v4-flash-vision-exp` (el único que ve imágenes) y goose
# sólo ofrece flash y pro. Medido: `session/set_config_option` con un id fuera de su lista SÍ se
# acepta, así que se inyectan desde aquí. Formato: `id|Nombre visible`, separados por coma.
ACP_EXTRA_MODELS=deepseek-v4-flash-vision-exp|DeepSeek V4 Flash · visión

# Cuáles de esos modelos ven imágenes. ACP no lo dice: `promptCapabilities.image` es del AGENTE
# y goose lo anuncia en true siempre; si el modelo elegido no ve, goose sustituye la imagen por
# `[image omitted: model does not support vision]` sin avisar a nadie. Vacío = se adivina por el
# nombre (`…-vision-…`, `…-vl-…`).
ACP_VISION_MODELS=deepseek-v4-flash-vision-exp

# Memoria episódica: usados por los scripts de respaldo/restauración (no por la UI).
# Ghosty Lite:
SESSION_DB_PATH=/data/ghosty/data/sessions/sessions.db
AGENT_SERVICE=ghosty-lite-runtime.service
# Goose manual: SESSION_DB_PATH=/data/state/goose/sessions/sessions.db
# y el servicio systemd real, p. ej. AGENT_SERVICE=goose-acp.service.

# Memoria procedural: repo público, sin credenciales embebidas.
SKILLS_REPO_URL=https://github.com/FrankIsmael/acp-agent-ui.git
SKILLS_REPO_BRANCH=main

ACP_EXTENSIONS_DB=.data/extensions.db

# ── Public demo (single server; replaceable guest policy) ────────────────────
# true opens the public guest demo and replaces the WhatsApp admin gate with
# per-browser ownership. false restores the existing app and WhatsApp gate.
PUBLIC_DEMO=false
DEMO_DB=.data/demo.db
# Shared allowance across web chat + linked WhatsApp; 1 conversation per guest.
# Estimated tokens, including repeated context, images and tool allowances.
DEMO_TOKEN_LIMIT=16000
DEMO_TURN_LIMIT=4
# Lifetime caps for this demo database, not daily limits.
DEMO_GLOBAL_TOKEN_LIMIT=500000
DEMO_USER_LIMIT=200
DEMO_CONTACT_URL=mailto:ismaelfcom93@gmail.com
# Existing WhatsApp owner gate, used only when PUBLIC_DEMO=false.
WHATSAPP_ADMIN_KEY=

# Per-IP fixed windows; persists across cookie clearing and server restarts.
DEMO_IP_GUEST_LIMIT=3
DEMO_IP_CHAT_LIMIT=10
DEMO_IP_WHATSAPP_LIMIT=5
DEMO_IP_API_LIMIT=120
# Only trust actual reverse-proxy IPs/CIDRs. Empty = direct TCP peer.
# For Caddy on this same host: loopback. Never use a universal subnet.
DEMO_TRUSTED_PROXIES=
